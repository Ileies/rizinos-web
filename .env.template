# Branding / SEO
PUBLIC_APP_NAME=RizinOS
PUBLIC_ORIGIN=rizinos.com

# Analytics
PUBLIC_GOOGLE_ANALYTICS_ID=
# PostHog is currently disabled in code; uncomment here and in +layout.ts to enable.
# PUBLIC_POSTHOG_KEY=

# Dev only: port of the rizinos Bun backend that vite proxies /api and /ws to.
# Must match PORT in the rizinos backend .env. Defaults to 3001 if unset.
BACKEND_PORT=3001
